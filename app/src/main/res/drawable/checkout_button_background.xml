<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/bottom_colors" />
            <corners android:radius="28dp" />
        </shape>
    </item>
    <!-- Normal state -->
    <item>
        <shape>
            <solid android:color="@color/bottom_colors" />
            <corners android:radius="28dp" />
        </shape>
    </item>
</selector>